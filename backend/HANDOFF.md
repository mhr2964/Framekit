# Backend Handoff

This backend slice is pinned to the accepted `/api/v1` room/comment surface.

## Canonical binding contract

The authoritative create→review→share binding spec for the current backend slice is:

- `workspace/docs/contracts/create-review-share-binding-spec.md`

Use that file as the source of truth for frontend/backend alignment. It intentionally flags unresolved share-route details instead of inventing them.